class UserNew {
  final String uid;
  UserNew({this.uid});
}

class UserData {
  final String uid, name, sugars;
  final int strength;
  UserData({this.sugars, this.strength, this.name, this.uid});
}
